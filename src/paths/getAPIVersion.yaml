description: Gets all guests data.
operationId: getAPIVersion
x-eov-operation-handler: endpoints
responses:
  "200":
    description: OK
    content:
      application/json:
        schema:
          $ref: '../index.yaml#/components/schemas/APIVersion'